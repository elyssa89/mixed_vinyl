
{% extends 'base.html.twig' %}
{% block content %}
    <h1>{{ title }}</h1>
    {# TODO: add an image of the record #}
    <div>
        Tracks:
        <ul>
            {% for track in tracks %}
                <li> 
                    {{ track['song'] }}
                </li>
            {% endfor %}
        </ul>
    </div>
{% endblock%}